/**
 * @repo/icons — tree-shakeable SVG icon set (§3).
 *
 * Convention: one SVG per file, PascalCase named export, `currentColor` fill +
 * `size` prop. This package holds CUSTOM brand glyphs (bee, hexagon) that lucide
 * doesn't provide; general UI icons continue to come from `lucide-react`.
 */

export { BeeMark } from "./BeeMark";
